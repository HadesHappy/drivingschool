import {
  ADD_ANSWER,
  UPDATE_ANSWER,
} from '../store/actions/constants'

const addAnswer = () => {
  return 0
}

const updateAnswer = () => {
  return 0
}
export {
  addAnswer,
  updateAnswer,
}

